# 自查清单

完成项目后，用这份清单判断自己是否真的掌握了，而不是只是把代码跑起来。

## 运行结果

- [ ] `pytest` 可以全部通过。
- [ ] `uvicorn app.main:app --reload` 可以启动服务。
- [ ] 浏览器可以打开 `http://127.0.0.1:8000/docs`。
- [ ] `GET /health` 返回 `{"status":"ok"}`。
- [ ] `/v1/chat` 可以返回聊天响应。
- [ ] `/v1/structured` 可以返回合法 JSON。
- [ ] `/v1/chat/stream` 可以看到多个 `data:` 片段。
- [ ] `/v1/usage` 可以看到调用次数和成本变化。

## 代码理解

- [ ] 我知道 `ChatRequest` 里每个字段的作用。
- [ ] 我知道 `LLMGateway` 为什么不直接写在路由函数里。
- [ ] 我知道 `ModelProvider` 抽象的价值。
- [ ] 我知道 Fake Provider 为什么适合放在 starter 项目里。
- [ ] 我知道结构化输出校验失败时为什么返回 `422`。
- [ ] 我能说清楚 Token、延迟和成本统计在哪里发生。

## 工程表达

- [ ] 我能用 1 分钟解释这个项目解决的问题。
- [ ] 我能说清楚它和普通聊天 Demo 的区别。
- [ ] 我能说出至少 3 个真实业务场景。
- [ ] 我能解释后续如何接入真实模型。
- [ ] 我能解释后续如何扩展到 Agent Tool Runtime。

## 作品集材料

- [ ] 我整理了项目 README 或作品集说明。
- [ ] 我保留了测试通过结果。
- [ ] 我保留了接口调用示例。
- [ ] 我写清楚了架构图或调用链路。
- [ ] 我写清楚了下一步扩展计划。

## 付费进阶前的最低标准

如果你准备继续做更完整的 AI Agent 工程项目，至少要达到：

- 能独立跑通这个项目。
- 能独立修改一个接口或测试。
- 能解释 Gateway 在 Agent 系统中的位置。
- 能说清楚为什么结构化输出、重试和成本统计是生产系统必需能力。

